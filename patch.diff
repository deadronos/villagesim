--- tests/unit/simEngine.unit.test.ts
+++ tests/unit/simEngine.unit.test.ts
@@ -165,4 +165,14 @@
       expect(npcsMixed.map(n => n.id)).not.toContain("npc-toma");
     });
   });
+
+  describe("applyNpcActionToTown", () => {
+    it("throws an error when the npcId does not exist in the town", () => {
+      const town = createMockTown({ id: "error-town" });
+
+      expect(() => {
+        applyNpcActionToTown(town, "non-existent-npc", { type: "wait" });
+      }).toThrowError("NPC non-existent-npc was not found in town error-town");
+    });
+  });
 });
